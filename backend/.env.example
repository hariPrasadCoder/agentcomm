# AgentComm Backend Environment Variables

# ============================================
# DATABASE MODE: "local" (SQLite) or "supabase"
# ============================================
# Set to "local" for quick testing without any cloud setup
# Set to "supabase" for production with Supabase
DB_MODE=local

# Local SQLite path (used when DB_MODE=local)
SQLITE_PATH=./agentcomm.db

# ============================================
# SUPABASE (only needed when DB_MODE=supabase)
# ============================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-role-key

# ============================================
# JWT (for local auth - change in production!)
# ============================================
JWT_SECRET=change-this-to-a-random-secret-in-production

# ============================================
# AI PROVIDER (at least one required)
# ============================================
ANTHROPIC_API_KEY=sk-ant-your-key
# OPENAI_API_KEY=sk-your-key

# Default AI Model
DEFAULT_PROVIDER=anthropic
DEFAULT_MODEL=claude-sonnet-4-20250514

# App Settings
DEBUG=true
